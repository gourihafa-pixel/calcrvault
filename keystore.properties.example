# Generated keystore.properties.example — copy to keystore.properties and edit.
# Used only for local release builds. Never commit real keystore.properties.
storeFile=keystore.jks
storePassword=YOUR_PASSWORD
keyAlias=YOUR_ALIAS
keyPassword=YOUR_PASSWORD
